{% do addHtmlToHead('
    <style>
    .mod_event-button-box .overflow-x-auto {
        overflow-x: auto;
    }
    </style>
') %}

<div class="mod_event-button-box">
    <nav class="overflow-x-auto">
        <ul class="d-inline-flex flex-nowrap">
            <li class="d-inline p-2 ps-0">
                {{ insert_tag('insert_module::'~keys.export_tour_list_module_id|default('999999999999'))|default('module not found')|raw }} {# Tourlist PDF Export #}
            </li>
            <li class="d-inline p-2">
                {{ insert_tag('insert_module::'~keys.export_tour_tech_diff_list_module_id|default('999999999999'))|default('module not found')|raw }} {# Schwierigkeitsgrade #}
            </li>
        </ul>
    </nav>
</div>
